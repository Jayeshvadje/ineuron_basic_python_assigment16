
# Question 2.Create a function that takes an angle in radians and returns the corresponding
# angle in degrees rounded to one decimal place.
import math
def rTOd(d):
    try:
        """function  that takes an angle in radians and returns the corresponding angle in degrees"""
        radiance= d*(180/3.14)
        print(radiance)
    except Exception as e:
        print(e)
rTOd(5)
